.venv/
.claude/
__pycache__/
*.pyc
.pytest_cache/
logs/*.log
downloads/
_handoff/
config/*.enc
*.json
!requirements.txt
!supabase/**/*.json
!webapp/package.json
!webapp/package-lock.json
!webapp/tsconfig.json
.env
supabase/.temp/

# Artefatos de build do PyInstaller (Fase 1, passo 1.3) — gerados
# localmente por `pyinstaller PainelOperador.spec`, nunca versionados
# (centenas de MB, inclui o Chromium do Playwright embutido).
/build/
/dist/

